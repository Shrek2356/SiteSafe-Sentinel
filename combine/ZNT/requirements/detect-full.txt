# 完整检测依赖（模式 2 云端 / 模式 3 本地）
# 注意：请先自行安装匹配 CUDA 的 PyTorch，再安装本文件
-r detect-bridge.txt
opencv-python>=4.8
ultralytics>=8.4
open_clip_torch>=2.24
pytest>=8.0
# SAM3（Windows 无 triton，项目内 edt.py 已提供 OpenCV 回退）
iopath>=0.1.10
huggingface_hub
ftfy>=6.1
regex
tqdm
# 建议：在仓库根目录执行
#   D:\Anaconda\envs\torch\python.exe -m pip install -e .\sam3-main
# torchvision必须与torch/CUDA成套安装，勿在此处单独指定版本。
