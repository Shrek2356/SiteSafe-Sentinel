# 桌面版 v1.4.2：规范证据链与全文知识库

2026-09-20。此版包含结构化规范引用、人工核验留痕、证据报告，以及200条可检索规范正文。GB条文说明独立保存，不作为强制条款。章节结构核验不等于全文逐字或法律效力核验。

## 安装与更新

- 首次使用：运行 `SiteSafe-Sentinel_Setup_v1.4.2_x64.exe`，或解压便携包后双击 `SiteSafe-Sentinel.exe`。
- 包含便携Python、编译后的前端和既有展示素材，不含模型权重、密钥与开发者业务数据库。真实模型仍按部署助手配置。
- 更新前退出软件并备份业务工作区。覆盖安装保留原有配置和知识库文件，不覆盖用户上传规范及核验记录。
- **要完整体验此次200条默认知识库，建议安装到新的目录，或直接使用本次便携包。**旧目录保留的 `source_catalog.json` 不会被强制覆盖，因此覆盖安装不能保证旧知识库自动切换到本版目录配置。不要为更新目录配置直接删除个人知识库；有自定义数据时应核对后合并。
- 本次生成新交付文件，不自动替换本机已安装的软件或快捷方式。

源码和说明同步GitHub；EXE、ZIP和便携运行环境不进入Git仓库。模型精度未重新测试。
